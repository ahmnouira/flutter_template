import 'package:flutter/material.dart';

class AppTextFieldSelect extends StatelessWidget {
  const AppTextFieldSelect({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
